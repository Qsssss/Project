export interface Room {
    id: number;
    komnaty: Array<object>;
    // photo: string;
    // description: string;
    // price: number;
    // chel: number;
    // hotel_id: number;
    // zanyat: boolean;
}
export interface Obj {
    
}